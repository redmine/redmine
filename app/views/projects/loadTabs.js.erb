$(function () {
	jQuery("#technology").append('<%= escape_javascript(render 'projects/technology', :locals => {:showcaseProjects => @showcaseProjects}) %>');
	jQuery("#groups").append('<%= escape_javascript(render 'projects/groups', :locals => {:groups => @groups}) %>');
	jQuery("#people").append('<%= escape_javascript(render 'projects/people', :locals => {:users => @users}) %>');		
	jQuery("#informationOSB").append('<%= escape_javascript(render 'projects/informationOSB') %>');
	
	// **************
	// EXPLORE PAGE
	// **************
	
	// -- Add filters
	setupFilter('#cellsfilter', '#cellslist');
	setupFilter('#groupsfilter', '#groupslist');
	setupFilter('#technologyfilter', '#technologylist');
	setupFilter('#peoplefilter', '#peoplelist');

	// -- Expands the first element of every list
	jQuery.each(jQuery(".projects-list"), function()
	{
		jQuery(this).find("a").first().click();
	});
	
});