<div class="contextual">
  <%= link_to_attachment @attachment, :text => "#{l(:button_download)} (#{number_to_human_size(@attachment.filesize)})", :download => true, :class => 'icon icon-download', :icon => "download" -%>
</div>

<h2>
  <%= safe_join([link_to_attachment_container(@attachment.container), @attachment.filename].compact, ' » ') %>
</h2>

<div class="attachments">
<p>
  <% if @attachment.description.present? %><%= @attachment.description %><br><% end %>
  <% content_type = content_tag(:span, @attachment.content_type, :class => 'content-type') if @attachment.content_type.present? %>
  <span class="author">
    <%= safe_join([link_to_user(@attachment.author),
                   format_time(@attachment.created_on),
                   content_type].compact, ', ') %>
  </span>
</p>
</div>
<div class="filecontent-container">
  <%= yield %>
</div>

<span class="pagination filepreview">
  <%= render_pagination %>
</span>

<% html_title @attachment.filename %>

<% content_for :header_tags do -%>
    <%= stylesheet_link_tag "scm" -%>
<% end -%>
