<div class="pull-right lead sidetitle"><%=l(:button_change_password)%> <i class="icon-key"></i></div>

<%= error_messages_for 'user' %>

<%= form_tag({}, :class => "tabular") do %>
	<div class="box">
		<div class="control-group">
			<label class="control-label" for="password"><%=l(:field_password)%> <span class="required">*</span></label>
			<div class="controls"><%= password_field_tag 'password', nil, :size => 25 %></div>
		</div>
		<div class="control-group">
			<label class="control-label" for="new_password"><%=l(:field_new_password)%> <span class="required">*</span></label>
			<div class="controls"><%= password_field_tag 'new_password', nil, :size => 25 %></div>
			<div class="popoverform"><icon class="popoverlink icon-2x icon-lightbulb"target="_blank" data-toggle="popover" data-trigger="hover" title="" data-content="<%= l(:text_caracters_minimum, :count => Setting.password_min_length) %>" data-original-title="Password"></icon></div>
		</div>
		<div class="control-group">
			<label class="control-label" for="new_password_confirmation"><%=l(:field_password_confirmation)%> <span class="required">*</span></label>
			<div class="controls"><%= password_field_tag 'new_password_confirmation', nil, :size => 25 %></div>
		</div>
	</div>
	<br/>
	<div class="control-group">
		<div class="nolabelcontrols">
			<%= submit_tag l(:button_apply), :class=>"btn btn-success btn-large" %>
		</div>
	</div>
<% end %>

<% unless @user.must_change_password? %>
<% content_for :sidebar do %>
<%= render :partial => 'sidebar' %>
<% end %>
<% end %>
